<!DOCTYPE html>
<html>
  <head>
    <title>
      <% if content_for?(:page_title) %>
        <%= yield(:page_title) %> | Never86'd
      <% else %>
        Never86'd"
      <% end %>
    </title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <% #stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
